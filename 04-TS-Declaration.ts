/*  
    ts的变量命名规则
    1. 变量名称可以包含数字和字母
    2. 除了下划线和美元符号外，不能包含其他空格字符，包括空格
    3. 变量名不能是数字开头 
*/

// 1. var [变量名] : [类型] = 值

var uname:string = 'charltin'

// 2. var [变量名] : [类型]
var age:number

// 3. var [变量名] = 值
var gender = 0

// 4. var [变量名]
var grade 
// 此时grade的类型是any，默认初始值是undefined

